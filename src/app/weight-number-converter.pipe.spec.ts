import { WeightNumberConverterPipe } from './weight-number-converter.pipe';

describe('WeightNumberConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new WeightNumberConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
